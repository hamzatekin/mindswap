services:
  mindswap:
    build: .
    restart: unless-stopped
    env_file: .env
    # bound to localhost only — Caddy on the host terminates HTTPS and proxies in
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/data
